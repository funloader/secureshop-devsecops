version: '3.8'
services:
  product-service:
    build: ./apps/product-service
    ports:
      - "5000:5000"
    # VULNERABLE: No security constraints

  order-service:
    build: ./apps/order-service  
    ports:
      - "3000:3000"
